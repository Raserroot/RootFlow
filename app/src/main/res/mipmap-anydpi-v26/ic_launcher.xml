<?xml version="1.0" encoding="utf-8"?>
<!--
  RootFlow 自适应图标（API 26+）—— 阶段 9

  结构：
  - background：`@color/rootflow_launcher_background`（深青 `#195464`，纯色，画满整层）
  - foreground：`@mipmap/ic_launcher_foreground`（五档密度的 PNG，**整张标志图**）

  ## ★ 2026-09-25 换标志（用户提供新图）
  新图是"深青底 + 终端 `>` + 绿色光标 + 电路线稿"，**底色铺满整张**。
  生成 foreground 时按自适应图标的规范把内容缩到 108dp 画布中间的 **72dp 安全区**，
  四周补上**与底色相同的** `#195464`（见 `values/colors.xml` 的说明）。

  ## 为什么不再需要抠图（与上一版标志的关键差别）
  旧标志是"青圆盘 + 白笔画"，圆盘比 background 小，整张当前景会叠出**两道可见边缘**，
  因此当时按亮度阈值（205）抠出白笔画、其余全透明。
  新标志的底色**铺满整张**，与 background 同色 ⇒ 直接整张当前景**不会**有接缝，抠图这步取消。

  ## 为什么 foreground 是 PNG 而不是 vector
  图形是**用户提供的位图标志**（终端提示符 + 电路线稿）。尝试过三条矢量化路线
  （手工贝塞尔拟合、按行提取中心线后串联、参数化圆环建模），在无矢量化工具
  （potrace 类）的环境下都无法忠实还原笔画连接，产出与原始标志明显不同。
  用户裁定：**直接用他提供的图**，并豁免"不用外部图片"那句约束。
  ⇒ 源图 445px 方形，缩到 xxxhdpi 的 288px 仍有余量，任何启动器尺寸都不会糊。

  ## 为什么没有 <monochrome>（Android 13+ 主题化图标）
  主题化要求前景是单色剪影并由系统按壁纸取色。加它会让"用户开启主题化图标"时
  品牌青丢失。当前选择是**保留品牌色**、不参与主题化（与 Magisk / LSPosed 一致）。
-->
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <background android:drawable="@color/rootflow_launcher_background" />
    <foreground android:drawable="@mipmap/ic_launcher_foreground" />
</adaptive-icon>
